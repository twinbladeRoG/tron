import ChatAgent from '@/components/modules/chat/ChatAgent';

const ChatPage = () => {
  return (
    <main className="flex h-full w-full overflow-hidden">
      <ChatAgent className="w-full" />
    </main>
  );
};

export default ChatPage;
