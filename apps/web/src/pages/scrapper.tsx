import Scrapper from '@/components/modules/scrapper/Scrapper';

const ScrapperPage = () => {
  return (
    <main className="flex h-full w-full overflow-hidden">
      <Scrapper className="w-full" />
    </main>
  );
};

export default ScrapperPage;
