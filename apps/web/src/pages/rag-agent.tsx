import RagAgent from '@/components/modules/agent/RagAgent';

const RagAgentPage = () => {
  return (
    <main className="flex h-full w-full overflow-hidden">
      <RagAgent className="w-full" />
    </main>
  );
};

export default RagAgentPage;
